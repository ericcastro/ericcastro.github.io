{
  "name": "ericcastro-github-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "clean": "rm -rf .astro dist",
    "preview": "astro preview",
    "pages:dev": "npx wrangler pages dev dist"
  },
  "dependencies": {
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
